

  <p><%= flash[:notice] %></p>


<%= render 'users/list', user: @book.user %>

<%= render 'books/list', book: Book.new %>



	<div class="col-xs-9">
<table class="table table-hover table-inverse">




<h2>Bookdatail</h2>
  <tr>

  <td> <%= attachment_image_tag(@book.user, :profile_image, :fill, 100, 100, format: 'jpeg', class: "pull-left profile-img", fallback: "no_image.jpg", size:"30x30") %>

      <td>
      <%= @book.title %>
      </td>

      <td>
      <%= @book.opinion %>
      </td>
      
      <% if @book.user_id == current_user.id %>

      <td>
      <%= link_to "edit", edit_book_path(@book.id), class: "btn btn-success edit_book_#{@book.id}" %>
      </td>

      <td>
        <%= link_to "Destroy", book_path(@book.id), method: :delete, "data-confirm" => "本当に削除しますか？", class: "btn btn-danger destroy_book_#{@book.id}" %>
      </td>

      <% end %>


    </tr>

      </tbody>
    </table>


